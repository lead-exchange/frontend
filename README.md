# Запуск

1. `npm install`
2. `npm run start` или `npm run start:dev` или `npm run start:e2e`

# Запуск тестов

## Подготовка
1. `pip install -r e2e/requirements.txt`
2. `playwright install`

## Запуск
1. Задать env переменные аналогично e2e/.env.example
2. `pytest e2e`
